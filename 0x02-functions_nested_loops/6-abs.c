#include "main.h"

/**
 * Entry - computes the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int
 */
int _abs/*computes the absolute value of an integer*/(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
